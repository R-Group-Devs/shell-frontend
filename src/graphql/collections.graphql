query collections($orderBy: Collection_orderBy) {
  collections(orderBy: $orderBy) {
    id
    name
    symbol
    address
    createdAtTimestamp
    lastUpdatedTimestamp
    factory { id address collectionCount }
    creator { id address }
    engine { id address }
  }
}
