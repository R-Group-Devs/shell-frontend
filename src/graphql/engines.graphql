fragment EngineInfo on Engine {
  id
  name
  address
  createdAtTimestamp
}

query engines(
  $orderBy: Engine_orderBy = createdAtTimestamp,
  $orderDirection: OrderDirection = desc)
{
  engines(orderBy: $orderBy, orderDirection: $orderDirection) {
    ... EngineInfo
  }
}
