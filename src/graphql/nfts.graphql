query getNfts(
  $orderBy: NFT_orderBy = tokenId
  $orderDirection: OrderDirection = asc
  $filter: NFT_filter
) {
  nfts(orderBy: $orderBy, orderDirection: $orderDirection, where: $filter) {
    ...NFTInfo
  }
}

query getNftDetails($nftId: ID!) {
  nft(id: $nftId) {
    ... NFTInfo
    fork {
      ... ForkInfo
    }
  }
}
