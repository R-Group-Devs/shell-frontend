query recentCollections {
  collections {
    id
    name
    symbol
    address
    createdAtTimestamp
    factory { id address collectionCount }
    creator { id address }
    engine { id address }
  }
}
