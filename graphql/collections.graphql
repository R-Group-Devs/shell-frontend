query recentCollections {
  collections {
    id
    name
    symbol
    address
    createdAtTimestamp
    lastUpdatedTimestamp
    factory { id address collectionCount }
    creator { id address }
    engine { id address }
  }
}
